server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # This is critical for single-page apps:
    # If a file doesn't exist, serve index.html instead
    # This lets React Router handle the routing client-side
    location / {
        try_files $uri $uri/ /index.html;
    }
}
